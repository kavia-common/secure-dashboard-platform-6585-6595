# secure-dashboard-platform-6585-6595

The backend container has been removed. The `main_frontend_app` now runs fully client-side using an in-memory mock auth service. See `main_frontend_app/README.md` for details on how the mock works and how to toggle to a live backend later.